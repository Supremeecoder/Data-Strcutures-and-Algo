#include <iostream>
#include<bits/stdc++.h>
using namespace std;
string findans(string str) {
    int len=str.size();
    if(len<=10) {
        return str;
    }
    string s="";
    s=s+str[0];
    ostringstream str1;
    str1 << len-2;
    string s1 = str1.str();
    s=s+s1;
    s=s+str[len-1];
    return s;
}
int main()
{
    int t;
    cin>>t;
    while(t--) {
        string str;;
        cin>>str;
        cout<<findans(str);
        cout<<endl;
    }
    return 0;
}