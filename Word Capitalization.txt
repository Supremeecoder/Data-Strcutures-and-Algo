#include <iostream>
#include<bits/stdc++.h>
using namespace std;
string findans(string s) {
    if(s[0]>=97 && s[0]<=122) {
        s[0]=s[0]-32;
    }
    return s;
}
int main()
{
        string s;
        cin>>s;
        cout<<findans(s);
    return 0;
}