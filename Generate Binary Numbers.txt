vector<string> generate(int N)
{
	queue<string> q;
	vector<string> ans;
	q.push("1");
	for(int i=0;i<N;i++) {
	    string res=q.front();
	    ans.push_back(res);
	    q.pop();
	    q.push(res+"0");
	    q.push(res+"1");
	}
	return ans;
}